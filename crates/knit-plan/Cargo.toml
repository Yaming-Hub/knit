[package]
name = "knit-plan"
version = "0.1.0"
edition = "2021"
description = "Execution planner and dependency resolver"

[dependencies]
knit-core = { path = "../knit-core" }
petgraph = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
siphasher = "1"
thiserror = "2"

[dev-dependencies]
